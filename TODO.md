# TODO

- [x] Update `DailySalesReport.jsx` so top metric cards support custom date ranges (day/range aware).
- [x] Fix missing `handlePrintRange()` and ensure range printing uses loaded range data.
- [x] Modify `loadHistoryForRange(from,to)` to compute/update range metrics (sales value, qty sold, remaining inventory estimate, best selling fruit) in addition to the history table.
- [x] Ensure today mode remains unchanged when no range is loaded.


